protocol 
1. login - sessionId(-1), form - userId
2. account_register - sessionId(-1), form - isAccountRegistered(bool)
3. logout - sessionId - None(for sessionId)
4. product_listup - [product list]
5. product_read - productNo - productInfo
6. product_register - form - isProdcutRegistered(bool)
7. product_delete - productNo
8. product_order - sessionId, productNo - isOrdered(bool)
9. product_myorderlist - sessionId - ordered product list
10. exit - sessionId

1. decode strip 은 request 를 가공하는 것 ; 문자열에서 앞뒤 공백 제거
2. json 기능 (json format data 처리법)
- import json 
- json.dumps(py_data) 를 통해 dict type py_data 를 object 로, list 나 tuple 을 array 로 관리 가능
- json.loads(json_file) 을 통해 역변환 가능
3. process() 기능
- target=fucntion, args=parameters 지정하여 fucntion(args) 형태로 실행하며 이 때 args 는 tuple 형태
- kwargs 를 하게 되면 dict 형태로 전달하게 된다 (key값에 대응되는 형태로 알아서 가져감)
- start()를 통해 실행
- join()을 통해 생성한 process 가 끝날 때까지 다른 process 는 대기
4. queue 
- FIFO 구조
- put() 을 하게 되면 요소 추가
- get() 을 하게 되면 최초의 요소가 제거됨
- a = queue[key] 를 하게 되면 해당 value 를 받아올 수 있음

명령 변환 (sessionID, userChoice), Route 변환 (convertedCommand)


